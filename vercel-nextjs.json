{
  "buildCommand": "cd dashboard-nextjs && npm install && npm run build",
  "outputDirectory": "dashboard-nextjs/.next",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/dashboard-nextjs/$1"
    }
  ]
}
